# War Heroes Photo Gallery — Руководство по настройке

Веб-приложение для просмотра отреставрированных исторических фотографий участников Второй мировой войны. Оптимизировано для мобильных устройств с функциями поиска и полноэкранного просмотра.

## 🎨 Дизайн

**Тема:** Living Memorial  
Теплая, уважительная эстетика с земляными тонами (кремовый, терракотовый, золотой) и элегантной типографией.

- **Цвета:** Кремовый фон (#FAF8F5), терракотовые акценты (#C85A3A), золотые детали (#D4A574)
- **Типография:** Playfair Display (заголовки), Open Sans (текст)
- **Анимации:** Плавные переходы (200-500ms), ленивая загрузка изображений

## ✨ Функции

### Галерея фотографий
- Адаптивная сетка (1 колонка на мобильных, 2 на планшетах, 3 на десктопе)
- Красивые карточки с фотографией, именем, рангом, подразделением и годами жизни
- Теги для категоризации (например, "Airborne", "Europe")

### Поиск
- Поиск в реальном времени по имени, рангу, подразделению и тегам
- Счётчик результатов
- Кнопка очистки поиска
- Состояние "нет результатов" с дружелюбным сообщением

### Просмотр фотографий
- Клик на карточку открывает полноэкранный просмотр
- Навигация стрелками (влево/вправо) или кнопками
- Поддержка клавиатуры (стрелки, Escape)
- Информация о фото в нижней части модального окна
- Счётчик (например, "1 / 6")

## 📱 Адаптивность

- **Мобильные устройства:** Оптимизировано для сенсорного управления, полная ширина
- **Планшеты:** 2 колонки, удобный размер карточек
- **Десктоп:** 3 колонки, полная функциональность

## 🚀 Как использовать

### 1. Добавление фотографий

Отредактируйте файл `client/src/pages/Home.tsx` и замените массив `SAMPLE_PHOTOS` на ваши данные:

```typescript
const SAMPLE_PHOTOS: HeroPhoto[] = [
  {
    id: '1',
    name: 'Иван Петров',
    imageUrl: 'https://your-image-url.jpg',
    rank: 'Капитан',
    unit: '101st Airborne Division',
    birthYear: 1920,
    deathYear: 1945,
    tags: ['Airborne', 'Europe'],
  },
  // Добавьте остальные фотографии...
];
```

**Поля:**
- `id` (обязательно) — уникальный идентификатор
- `name` (обязательно) — имя участника
- `imageUrl` (обязательно) — URL фотографии
- `rank` — воинское звание
- `unit` — подразделение
- `birthYear` — год рождения
- `deathYear` — год смерти
- `description` — описание (опционально)
- `tags` — теги для поиска

### 2. Загрузка изображений

**Вариант 1: Использование CDN (рекомендуется)**
- Загрузите фотографии на облачный сервис (Imgur, Cloudinary, AWS S3)
- Используйте прямые ссылки в поле `imageUrl`

**Вариант 2: Локальные файлы**
- Поместите изображения в папку `client/public/images/`
- Используйте пути вроде `/images/photo1.jpg`

### 3. Запуск локально

```bash
# Установка зависимостей
pnpm install

# Запуск сервера разработки
pnpm dev

# Открыть http://localhost:3000
```

### 4. Развертывание

Приложение готово к развертыванию на любой платформе:

**Manus (встроенный хостинг):**
- Нажмите кнопку "Publish" в интерфейсе Manus
- Получите автоматический домен или подключите свой

**Vercel/Netlify:**
```bash
pnpm build
# Загрузите папку dist на платформу
```

**Docker:**
```bash
pnpm build
npm start
```

## 🔧 Структура проекта

```
client/
  src/
    pages/
      Home.tsx          ← Главная страница с галереей
    components/
      PhotoCard.tsx     ← Карточка фотографии
      PhotoModal.tsx    ← Модальное окно просмотра
      SearchBar.tsx     ← Поиск
      EmptyState.tsx    ← Состояние "нет результатов"
    hooks/
      useGallery.ts     ← Логика управления галереей
    index.css           ← Глобальные стили и тема
shared/
  types.ts              ← TypeScript типы
```

## 📊 Типы данных

```typescript
interface HeroPhoto {
  id: string;
  name: string;
  imageUrl: string;
  rank?: string;
  unit?: string;
  birthYear?: number;
  deathYear?: number;
  description?: string;
  tags?: string[];
}
```

## 🎯 Рекомендации

### Для лучшего результата:
1. **Размер изображений:** Оптимальный размер 400x400px (квадратные)
2. **Формат:** JPG или WebP для быстрой загрузки
3. **Качество:** Убедитесь, что фотографии хорошо отреставрированы
4. **Данные:** Заполните все доступные поля для лучшего поиска

### Производительность:
- Изображения загружаются лениво (lazy loading)
- Поиск оптимизирован для 40+ фотографий
- Модальное окно использует CSS для плавных переходов

## 🌐 Поддержка браузеров

- Chrome/Edge 90+
- Firefox 88+
- Safari 14+
- Mobile Safari (iOS 14+)
- Chrome Mobile (Android 5+)

## 💡 Дополнительные идеи

Вы можете расширить функциональность:
- Добавить фильтры по рангу или подразделению
- Реализовать сортировку (по имени, году)
- Добавить экспорт в PDF
- Интегрировать с базой данных для управления фотографиями
- Добавить комментарии или истории участников

---

**Создано с ❤️ для сохранения исторической памяти**
